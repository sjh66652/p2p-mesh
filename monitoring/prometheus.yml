# P2P Mesh Network - Prometheus Configuration
# Scrapes metrics from the control plane API and export targets.

global:
  scrape_interval: 15s
  scrape_timeout: 10s
  evaluation_interval: 15s

  external_labels:
    cluster: p2p-mesh
    environment: production

# Alerting rules
rule_files:
  - "alerts/*.yml"

scrape_configs:
  # Auth Service
  - job_name: "mesh-auth"
    metrics_path: /metrics
    static_configs:
      - targets:
          - "auth-service:8000"
        labels:
          service: "auth"
          component: "microservice"

  # User Service
  - job_name: "mesh-user"
    metrics_path: /metrics
    static_configs:
      - targets:
          - "user-service:8000"
        labels:
          service: "user"
          component: "microservice"

  # Signaling Service
  - job_name: "mesh-signaling"
    metrics_path: /metrics
    static_configs:
      - targets:
          - "signaling-service:8000"
        labels:
          service: "signaling"
          component: "microservice"

  # Relay Service
  - job_name: "mesh-relay-svc"
    metrics_path: /metrics
    static_configs:
      - targets:
          - "relay-service:8000"
        labels:
          service: "relay"
          component: "microservice"

  # Usage Service
  - job_name: "mesh-usage"
    metrics_path: /metrics
    static_configs:
      - targets:
          - "usage-service:8000"
        labels:
          service: "usage"
          component: "microservice"

  # Node exporter (if installed on relay hosts)
  - job_name: "node"
    static_configs:
      - targets:
          - "relay:9100"
        labels:
          service: "relay"

  # Prometheus self-monitoring
  - job_name: "prometheus"
    static_configs:
      - targets:
          - "localhost:9090"

  # Loki self-monitoring
  - job_name: "loki"
    metrics_path: /metrics
    static_configs:
      - targets:
          - "loki:3100"
        labels:
          service: "loki"

  # Jaeger self-monitoring
  - job_name: "jaeger"
    metrics_path: /metrics
    static_configs:
      - targets:
          - "jaeger:16686"
        labels:
          service: "jaeger"

# Alertmanager (optional)
# alerting:
#   alertmanagers:
#     - static_configs:
#         - targets: ["alertmanager:9093"]
